{
  "require": {
    "league/flysystem-aws-s3-v3": "^3.0",
    "maatwebsite/excel": "^3.1",
    "owen-it/laravel-auditing": "^14.0",
    "robsontenorio/mary": "^2.4",
    "spatie/laravel-permission": "^6.16",
    "spatie/laravel-query-builder": "^6.3"
  },
  "require-dev": {
    "barryvdh/laravel-ide-helper": "^3.5",
    "roave/security-advisories": "dev-latest"
  },
  "scripts": {
    "ci": [
      "@composer phpcs",
      "@composer rector",
      "@composer larastan",
      "@composer test"
    ],
    "ide-helper-update": [
      "@php artisan clear-compiled",
      "@php artisan ide-helper:generate",
      "@php artisan ide-helper:meta",
      "@php artisan ide-helper:models --write --reset",
      "@composer phpcs-fix"
    ],
    "larastan": [
      "@php vendor/bin/phpstan analyse -v"
    ],
    "laravel-cache": [
      "@php artisan view:cache"
    ],
    "phpcs": [
      "php-cs-fixer check -v"
    ],
    "phpcs-fix": [
      "php-cs-fixer fix"
    ],
    "post-autoload-dump": [
      "Illuminate\\Foundation\\ComposerScripts::postAutoloadDump",
      "@php artisan package:discover --ansi"
    ],
    "post-update-cmd": [
      "@php artisan clear-compiled",
      "@php artisan ide-helper:generate",
      "@php artisan ide-helper:meta",
      "@php artisan vendor:publish --tag=laravel-assets --ansi --force"
    ],
    "post-production-deploy": [
      "@php artisan config:cache",
      "@php artisan migrate:fresh --force --seed"
    ],
    "rector": [
      "@php vendor/bin/rector --dry-run"
    ],
    "rector-fix": [
      "@php vendor/bin/rector"
    ],
    "test": [
      "phpunit"
    ]
  },
  "extra": {
    "laravel": {
      "dont-discover": [
        "laravel/telescope"
      ]
    }
  },
  "scripts-descriptions": {
    "ci": "These are the CI tests and code standards tools that run.",
    "larastan": "Runs the PHPStan with Laravel plugin.",
    "laravel-cache": "This runs all of the Laravel cache functionality besides config. (Config is off because that is part of a container start up using ENV vars.)",
    "phpcs": "The PHP Code Sniffer tester.",
    "phpcs-fix": "Automatically fixes any sniffs it can.",
    "post-production-deploy": "Tasks that need to be done for Laravel after the container is launched but before it's ready.",
    "rector": "Runs Rector and reports errors",
    "rector-fix": "Runs Rector and automatically fixes errors",
    "test": "PHPUnit tests for Laravel."
  }
}
